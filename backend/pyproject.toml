[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
per-file-ignores = { "alembic/env.py" = ["E402"] }

[tool.ruff.lint.isort]
known-first-party = ["app"]
